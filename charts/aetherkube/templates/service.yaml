apiVersion: v1
kind: Service
metadata:
  name: {{ include "aetherkube.fullname" . }}
  labels:
    {{- include "aetherkube.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aetherkube.selectorLabels" . | nindent 4 }}
